# Copy to web/.env.local (git-ignored). Vite only exposes VITE_-prefixed vars.
VITE_SUPABASE_URL=https://dabgpgjwvarojclnpptf.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
# Where the FastAPI write API is reachable (local dev default below).
VITE_API_URL=http://localhost:8000
